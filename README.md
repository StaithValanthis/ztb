# ztb — Zero Alpha Trading Bot

Single, reusable, deterministic trading-bot platform for **Bybit only**. Build the engine first, strategies as plugins.

## Quick start

```bash
pip install -e ".[dev]"
ztb --help
ztb --version
```

## License

MIT
