# Head Of Research — instructions moved out of this repo

**This file is NOT loaded. It is intentionally a stub.**

The live instructions the agents actually load are in the stable mirror, OUTSIDE
this repo's volatile git:

    /home/ubuntu/ztb-firm/agents/head-of-research.md

Edit them THERE (see /home/ubuntu/ztb-firm/README.md). This stub exists only so an
operator who opens this familiar path is not fooled into editing a dead copy — the
firm's git churn (detached HEAD / resets in this shared tree) reverts anything here.
